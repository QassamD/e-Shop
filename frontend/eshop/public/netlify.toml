[build]
  command = "yarn build && yarn typecheck"
  publish = "dist"
  environment = { NODE_ENV = "production" }

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true

[build.environment]
  NODE_ENV = "production"

[build.environment]
  NODE_ENV = "production"

